<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.khnp.pels.system.qr">
	<select id="QRList" resultType="java.util.HashMap" parameterType="java.util.HashMap">
		SELECT * FROM (
			SELECT ROW_NUMBER() OVER (ORDER BY A.RG_DT DESC) AS LISTNUM,
				B.PRCDOC_UNQ_KY_VAL,
				A.DIVS_CD,
				A.PPCD,
				B.LOCT_NM,
				B.QR_CD_INFO,
				A.PRCDOC_CFY,
				A.PRCDOC_NO,
				A.PRCDOC_NM,
				A.DOC_TYP,
				A.DOC_PART_NO,
				A.RRD_CFY,
				A.PRCDOC_RVSN_NO,
				A.ATCT_NM,
				A.FNCLC_ID,
				B.REGPR_ID,
				B.REGPR_NM,
				B.RG_DT,
				TO_CHAR(B.RG_DT, 'YYYY-MM-DD') AS FM_RG_DT
			FROM GE_MP_PRCDOC_M A, GE_MP_QR_M B
			WHERE A.PRCDOC_UNQ_KY_VAL = B.PRCDOC_UNQ_KY_VAL
			  AND A.PRCDOC_UNQ_KY_VAL =  #{PRCDOC_UNQ_KY_VAL}
			ORDER BY B.RG_DT DESC
		)
		WHERE LISTNUM BETWEEN #{DISPSTART} AND #{DISPEND}		
	</select>
	
	<select id="QRCount" resultType="int" parameterType="java.util.HashMap">
		SELECT 
			COUNT(*) 
			FROM GE_MP_PRCDOC_M A, GE_MP_QR_M B
			WHERE A.PRCDOC_UNQ_KY_VAL = B.PRCDOC_UNQ_KY_VAL
			  AND A.PRCDOC_UNQ_KY_VAL =  #{PRCDOC_UNQ_KY_VAL}
	</select>
	
	<insert id="InsertQR" parameterType="java.util.HashMap">
		INSERT INTO GE_MP_QR_M
		(
			PRCDOC_UNQ_KY_VAL,
			LOCT_NM,
			QR_CD_INFO,
			REGPR_ID,
			REGPR_NM,
			RG_DT
		)
		VALUES
		(
			#{PRCDOC_UNQ_KY_VAL},
			#{LOCT_NM},
			#{QR_CD_INFO},
			#{REGPR_ID},
			#{REGPR_NM},
			SYSDATE
		)
	</insert>	
	
	<delete id="DeleteQR" parameterType="java.util.HashMap">
		DELETE FROM GE_MP_QR_M
		 WHERE PRCDOC_UNQ_KY_VAL = #{PRCDOC_UNQ_KY_VAL}
		   AND LOCT_NM = #{LOCT_NM}
	</delete>	
	
</mapper>